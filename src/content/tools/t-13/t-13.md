---
uid: "t-13"
introduction: "通过 notion-cli 管理 Notion 页面和数据库"
---

# NOTION

使用 Notion API 创建、读取、更新页面、数据源和块。

## SETUP

1. 在 https://notion.so/my-integrations 创建集成
2. 复制 API 密钥（以 `ntn_` 或 `secret_` 开头）
3. 存储密钥：

```bash
mkdir -p ~/.config/notion
echo "ntn_your_key_here" > ~/.config/notion/api_key
```

4. 将目标页面/数据库与你的集成共享（点击 "..." → "Connect to" → 你的集成名称）

## API BASICS

所有请求需要：

```bash
NOTION_KEY=$(cat ~/.config/notion/api_key)
curl -X GET "https://api.notion.com/v1/..." \
  -H "Authorization: Bearer $NOTION_KEY" \
  -H "Notion-Version: 2025-09-03" \
  -H "Content-Type: application/json"
```

> Note: Notion-Version 请求头是必需的。此技能使用 2025-09-03（最新版本）。在此版本中，数据库在 API 中称为"data sources"。

## COMMON OPERATIONS

搜索页面和数据源：

```bash
curl -X POST "https://api.notion.com/v1/search" \
  -H "Authorization: Bearer $NOTION_KEY" \
  -H "Notion-Version: 2025-09-03" \
  -H "Content-Type: application/json" \
  -d '{"query": "page title"}'
```

获取页面：

```bash
curl "https://api.notion.com/v1/pages/{page_id}" \
  -H "Authorization: Bearer $NOTION_KEY" \
  -H "Notion-Version: 2025-09-03"
```

获取页面内容（块）：

```bash
curl "https://api.notion.com/v1/blocks/{page_id}/children" \
  -H "Authorization: Bearer $NOTION_KEY" \
  -H "Notion-Version: 2025-09-03"
```

在数据源中创建页面：

```bash
curl -X POST "https://api.notion.com/v1/pages" \
  -H "Authorization: Bearer $NOTION_KEY" \
  -H "Notion-Version: 2025-09-03" \
  -H "Content-Type: application/json" \
  -d '{
    "parent": {"database_id": "xxx"},
    "properties": {
      "Name": {"title": [{"text": {"content": "New Item"}}]},
      "Status": {"select": {"name": "Todo"}}
    }
  }'
```

查询数据源（数据库）：

```bash
curl -X POST "https://api.notion.com/v1/data_sources/{data_source_id}/query" \
  -H "Authorization: Bearer $NOTION_KEY" \
  -H "Notion-Version: 2025-09-03" \
  -H "Content-Type: application/json" \
  -d '{
    "filter": {"property": "Status", "select": {"equals": "Active"}},
    "sorts": [{"property": "Date", "direction": "descending"}]
  }'
```

## PROPERTY TYPES

数据库项的常用属性格式：

- Title: `{"title": [{"text": {"content": "..."}}]}`
- Rich text: `{"rich_text": [{"text": {"content": "..."}}]}`
- Select: `{"select": {"name": "Option"}}`
- Multi-select: `{"multi_select": [{"name": "A"}, {"name": "B"}]}`
- Date: `{"date": {"start": "2024-01-15", "end": "2024-01-16"}}`
- Checkbox: `{"checkbox": true}`
- Number: `{"number": 42}`
- URL: `{"url": "https://..."}`
- Email: `{"email": "a@b.com"}`
- Relation: `{"relation": [{"id": "page_id"}]}`

## KEY DIFFERENCES IN 2025-09-03

- **Databases → Data Sources**: 使用 `/data_sources/` 端点进行查询和检索
- **Two IDs**: 每个数据库现在同时拥有 `database_id` 和 `data_source_id`
  - 创建页面时使用 `database_id`（`parent: {"database_id": "..."}`）
  - 查询时使用 `data_source_id`（`POST /v1/data_sources/{id}/query`）
- **Search results**: 数据库以 `"object": "data_source"` 形式返回，带有 `data_source_id`
- **Parent in responses**: 页面显示 `parent.data_source_id` 和 `parent.database_id`
- **Finding the data_source_id**: 搜索数据库，或调用 `GET /v1/data_sources/{data_source_id}`

## NOTES

- 页面/数据库 ID 是 UUID（可以有或没有连字符）
- API 无法设置数据库视图过滤器 — 那是仅 UI 功能
- 速率限制：平均约 3 请求/秒
- 创建数据源时使用 `is_inline: true` 将其嵌入页面